'use strict';

$(document).ready(function () {
    $('#shortDescription').on('keyup', function () {
        $('#shortDescription').attr('maxlength', 55);
    });
    $('#shortDescription').attr('maxlength', 55);
})
